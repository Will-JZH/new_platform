<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jzh.entity">

	<resultMap type="com.jzh.entity.AtomicService" id="atomicServiceResult">
		<result property="id" column="id"/>
		<result property="serviceName" column="serviceName"/>
		<result property="price" column="price"/>
        <result property="procTime" column="procTime"/>
	</resultMap>
	
	<select id="getAtomicByServiceName" parameterType="com.jzh.entity.AtomicService" resultMap="atomicServiceResult">
		SELECT * FROM atomicService WHERE serviceName LIKE CONCAT(#{userName}, '%')
	</select>
	
	<select id="getAtomicByEnterpriseName" parameterType="com.jzh.entity.AtomicService" resultMap="atomicServiceResult">
		SELECT * FROM atomicService WHERE serviceName LIKE CONCAT('%', #{userName})
	</select>
	
	<select id="getAllUsers" parameterType="User" resultMap="UserResult">
		select * from t_user
	</select>
	
	<insert id="addUser" parameterType="User">
		insert into t_user (userName) values (#{userName});
	</insert>
	
	<delete id="deleteUser" parameterType="java.lang.String">
		delete from t_user where userName = #{userName}
	</delete>
	
	<delete id="deleteAll" parameterType="java.lang.String">
		delete from t_user
	</delete>
	

</mapper> 